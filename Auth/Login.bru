meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/users/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "email": "admin_penawaran@jli.co.id",
    "password": "helloworld"
  }
}

script:post-response {
  bru.setVar("AUTH_TOKEN", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
