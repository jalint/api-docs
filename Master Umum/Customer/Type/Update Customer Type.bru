meta {
  name: Update Customer Type
  type: http
  seq: 5
}

patch {
  url: {{BASE_URL}}/customers/types/8
  body: json
  auth: inherit
}

body:json {
  {
    "name": "SWASTA"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
