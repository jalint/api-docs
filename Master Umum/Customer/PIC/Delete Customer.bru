meta {
  name: Delete Customer
  type: http
  seq: 1
}

delete {
  url: {{BASE_URL}}/customers/{customer}/contacts
  body: none
  auth: inherit
}

body:json {
  {
    "name": "Perusahaan"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
